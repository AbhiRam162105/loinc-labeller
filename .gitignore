# secrets — never commit real credentials
.streamlit/secrets.toml

# labels are written to the Google Sheet (or a local CSV fallback) — not committed
diagnosis_labels.csv
*.tmp

# python
__pycache__/
*.pyc
venv/
.venv/
